# Persona: terminal-developer--interactive

Reuse: `terminal-developer--interactive`, base 2026-09-17 (durable collection
`givn/personas/terminal-developer--interactive.md`, promoted from the
`terminal-wow-factor` topic); this file adds record entries only.

## Topic stances

### 2026-09-24 — visible-request-cost

- **Cost stance.** This developer pays the provider bill for their own
  requests and weighs a decision partly by what it costs. The existing
  interests already carry the shape — "move from intent to useful action
  without breaking concentration" and "keep the terminal history useful" — but
  neither names money, and the recorded success criterion is about trust and
  safety, not spend.
- **What this changes.** An amount the developer pays for must reach them
  inside the pane that asks for the next decision, not on a line that scrolls
  away; a number whose absence is silent is not a number they can act on.
- **What this does not change.** The persona's authority boundary (decides what
  reaches the shell) and its evaluation capability (judges whether a proposed
  action is trustworthy) are unchanged; no spending authority, budget, or
  accounting responsibility is added.

## Reuse justification

Matched by ID against the durable collection and the sibling topic's
`personas/` inventory. No new segment is justified: the cost-aware developer
shares the existing persona's authority boundary (decides what reaches the
shell) and evaluation capability (judges a proposed action), and adds only a
topic-specific demand, which belongs in a dated stance block rather than a new
persona file.
