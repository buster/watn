# Domain Terms: visible-request-cost

| Term | Status | Definition | Contrast | First use | Collision check |
|---|---|---|---|---|---|
| Price | candidate | The provider-quoted unit rate for one model, expressed in USD per one million tokens. | Not the amount a request actually incurred. | `docs/arc42/12-glossary.md:18` (`Pricing`), `:19` (`Catalog price`), `:20` (`Price capture`); user request "show the price of the actions i take". | `grep -rInic "price" givn/specs/ src/ docs/arc42/` → specs: `configure-model/models.feature:3`, `ratatui-model-picker.feature:6`, `streamlined-setup.feature:2`; source: `src/models/mod.rs:1`, `src/setup.rs:3`; all hits are the recorded per-million rate for catalog models. The user's "price of the actions i take" means an incurred amount, not a rate: same word, two meanings. |
| Cost estimate | candidate | The amount computed for one completed request from its token usage and the recorded per-million price. | Not a provider invoice and not a prediction of future spend. | `givn/specs/fragments/config.feature:44`, `givn/specs/fragments/ask.feature:60` (`cost value`), `src/output/render.rs:72`. | `grep -rInic "cost estimate" givn/specs/ src/ docs/arc42/` → one permanent hit, `givn/specs/fragments/config.feature:1`. No competing usage; the phrase is not yet durable in `docs/arc42/12-glossary.md`. |
| Amount | confirmed | The money the provider billed for one completed request: reported tokens × the recorded per-million price of the model the provider reported, in USD, displayed in cents. | Not a rate, not a prediction, and not a session total; a request that never completed has no amount. | Promoted to `docs/arc42/12-glossary.md` 2026-09-24; `givn/specs/fragments/config.feature:44` (`cost estimate`), `givn/specs/fragments/ask.feature:63` (`cost value`). | `grep -rInic "cost estimate" givn/specs/ src/ docs/arc42/` → one permanent hit, `givn/specs/fragments/config.feature:1` file count. `grep -rInic "amount" docs/arc42/12-glossary.md` → 0 hits before promotion: the durable glossary had no term for the incurred amount, only `Pricing`, `Catalog price`, and `Price capture` (lines 18-20). Promoted by user decision 2026-09-24. |
| Proposed action | rejected | The persona's word for the command Watn offers. | Not a request and not a billable event. | `givn/personas/terminal-developer--interactive.md:40`, `:42`. | `grep -rInic "proposed action" givn/specs/ givn/personas/ givn/ideation/ src/ docs/arc42/` → hits only in personas (`givn/personas/terminal-developer--interactive.md:2`, `givn/ideation/terminal-wow-factor/personas/terminal-developer--interactive.md:2`) and one event-storming line; zero in permanent specs and source. Rejected for promotion 2026-09-24 (user): the specs use `Candidate` and `Review decision`, so the word stays in the persona record and never enters the specs as a synonym. |
